/////////////////////////////////////////////////////////////////////////////////////////////////////////////////
$(document).ready(function() {

    // initial setup for flowtype
    $('body').flowtype({
        minFont: 6,
        maxFont: 16,
        fontRatio: 45
    });

    $('#membersSlide').flowtype({
        minFont: 8,
        maxFont: 14,
        fontRatio: 32
    });
    $('.angle-text ac_collapse').flowtype({
        minFont: 8,
        maxFont: 20
    });
    /*
        $('.navbar-brand').flowtype({
            minFont: 7,
            maxFont: 40,
            fontRatio: 67
        */

});
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////
